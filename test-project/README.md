# Test Project

A minimal C++ hello-world project used to validate the `pipery-cpp-ci` action.

## Contents

- `CMakeLists.txt` — CMake build definition
- `src/main.cpp` — Hello-world entry point

## Building locally

```bash
cmake -B build .
cmake --build build
./build/hello-pipery-cpp
```
